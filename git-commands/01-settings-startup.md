# Git Commands - Settings and Startup

The following commands configure and set up the Git environment.

**Cheat Sheet**:

| Command    | Description                                        |
| ---------- | -------------------------------------------------- |
| git config | Configure Git settings (name, email, editor, etc.) |
| git init   | Initialize a new Git repository                    |
| git clone  | Clone an existing remote repository                |